# ============================================================================
# DEVCONTAINER SERVICE CONFIGURATION
# ============================================================================
# Enable or disable optional services by setting to true/false
# After making changes: Rebuild the container
#   - In VS Code: Cmd+Shift+P → "Dev Containers: Rebuild Container"
#   - Or manually: docker-compose down && docker-compose up -d
# ============================================================================

# ----------------------------------------------------------------------------
# OPTIONAL SERVICES
# ----------------------------------------------------------------------------

# Playwright Service
# Browser automation with Chromium via HTTP API
# Use case: Web scraping, UI testing, screenshot capture
ENABLE_PLAYWRIGHT=true

# FastAPI Service (Future)
# FastAPI development server with hot reload
# Use case: API development, backend services
ENABLE_FASTAPI=false

# MCP Server (Future)
# Model Context Protocol server
# Use case: AI/ML model serving, Claude integration
ENABLE_MCP=false

# ----------------------------------------------------------------------------
# SERVICE CONFIGURATION
# ----------------------------------------------------------------------------

# Port configuration for services
PLAYWRIGHT_PORT=3000
FASTAPI_PORT=8000
MCP_PORT=8080

# ----------------------------------------------------------------------------
# NOTES
# ----------------------------------------------------------------------------
# - Core workspace service is ALWAYS enabled (Python, Jupyter, Docker CLI)
# - Optional services run in separate containers for isolation
# - Disabled services won't consume resources
# - You can enable services individually or in combination
#
# Examples:
#   Full stack: ENABLE_PLAYWRIGHT=true ENABLE_FASTAPI=true
#   Minimal: All services set to false (core only)
#   Browser only: ENABLE_PLAYWRIGHT=true (others false)
# ============================================================================
